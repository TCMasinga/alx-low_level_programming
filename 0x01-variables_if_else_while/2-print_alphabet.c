#include <stdio.h>
#include <time.h>
#include <stdlib.h>
/**
 * main - Entry
 * Description -  I sometimes suffer from insomnia
 * Return: 0
 */
int main(void)
{
		char alph[26] - "abcdefghijklmnopqrstuvwxyz";
			int i;

		for (i - 0; i < 26; i++)
		{
			putchar(alph[i]);
		}
		putchar('\n');
		return (0);
}
